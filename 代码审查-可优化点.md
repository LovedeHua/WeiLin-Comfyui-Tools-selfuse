> 审查对象：WeiLin-Comfyui-Tools-selfuse（ComfyUI 自定义节点，自用 fork）
> 规模：前端 53 文件 / 28103 行（Vue3 + Vite），后端 42 文件 / 6346 行（aiohttp + SQLite）
> 方法：静态代码分析 + 关键问题逐条回源码核对（标注 ✅ 的为我亲自核实）
> 日期：2026-09-09

---

## 一、已核实的高危问题（建议优先修）

### 1. 事件监听器永久泄漏 ✅
`src/src/components/FloatingBall.vue:164-165` / `209-210`
```js
document.addEventListener('mousemove', (e) => onDrag(e, i));     // 匿名函数
document.removeEventListener('mousemove', (e) => onDrag(e, i));   // 新匿名函数 ≠ 旧引用
```
两个箭头函数是**不同引用**，`removeEventListener` 永远失效 → 每次拖拽都往 document 叠加一对 mousemove/mouseup 监听，拖 100 次就是 200 个常驻监听器，越拖越卡。
**改法**：用命名函数或 `AbortController`（`{signal}`），并在 `onBeforeUnmount` 兜底清理。

### 2. 静态文件路由存在路径穿越 ✅
`app/server/prompt_server.py:46-53`
```python
file_path = request.match_info.get('file_path', '')
full_path = os.path.join(static_path, file_path)   # 无 realpath / 前缀校验
```
`GET /weilin/prompt_ui/file/../../../../etc/passwd` 可读任意文件。
**改法**：`os.path.realpath(full_path)` 后断言 `startswith(os.path.realpath(static_path))`，否则 403。

### 3. 任意 SQL 执行接口（无鉴权）✅
`app/server/prompt_api/tags_manager.py:345-357`（`run_sql_text` 直接 `cursor.execute(sql)` 执行前端传入语句）
路由：`prompt_server.py:379` `POST /prompt/run_sql_text`，danbooru 同理 `danbooru.py:157` + `prompt_server.py:1094`。
**改法**：直接删除该接口；若确需维护能力，改为白名单枚举的固定操作。

### 4. 同步阻塞调用跑在事件循环里 ✅
`prompt_api/lora_info.py:23/355/542` 的 `requests.get`；`dao/dao.py:367/720/736/777` 的 `requests.get`。
协程内同步网络请求会**冻结整个 ComfyUI 界面**；`lora_info.py:624` 对 GB 级模型文件逐块算 SHA256 同样阻塞。
**改法**：`requests` → `aiohttp`；文件哈希/IO 用 `loop.run_in_executor`；哈希结果落缓存避免重复计算。

---

## 二、高危（架构/并发层）

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 5 | 数据库连接跨协程共享，仅"创建"加锁、`execute` 未加锁；`atexit` 注册的是 `async def`（永不被 await，连接关不掉） | `dao/dao.py:19-72, 857-862` ✅ | 每请求 `async with aiosqlite.connect(...)`，或用队列串行化 |
| 6 | `postMessage` 全部未校验 `event.origin`，任何父页面可驱动内部动作 | `App.vue:286`、`FloatingBall.vue:348`、`tag_index.vue:744`、`prompt_index.vue` 等 | 白名单 origin 校验 |
| 7 | `watch(tokens, ..., {deep:true})` + 全量 `v-for` 无虚拟滚动，长 prompt 卡顿 | `prompt_index.vue:2468`、`模板 :352` | 只监听必要字段；引入虚拟滚动；`:key` 用稳定 id |
| 8 | 标签列表全量渲染无分页/虚拟化 | `tag_index.vue:224` | 复用 `lora_index.vue` 已有的 `paginatedLoraList` 分页方案 |
| 9 | `loading_status` 由线程池写、事件循环读，存在竞态 | `prompt_api/lora_networks.py:158` | `asyncio.Lock` 或事件队列 |

---

## 三、中优先级

10. **外部调用缺超时**：`translate/network_translate/alibabav2.py:314` `timeout=kwargs.get('timeout', None)` 默认无超时，可能永久挂起。统一 `timeout=10`。✅（已确认多处 requests 无 timeout）
11. **裸 except**：`dao/dao.py:865`、`lora_info.py:86/107`、`random_tag_template.py:118` —— 吞掉所有异常，排障困难。
12. **错误处理样板化**：`prompt_server.py` 约 40 个路由均为 `except Exception: print(...); return 500`，无结构化错误体。建议抽 `@api_handler` 装饰器统一 try / 日志 / 错误码。
13. **SQL 列名拼接**：`danbooru.py:92-99` `SET {field}=?` 字段名未白名单校验。
14. **同步 sqlite 混入异步路由**：`tags_manager.py:173/206` 用 `sqlite3.connect`，每次新建连接且阻塞循环。
15. **全量返回无缓存/分页**：`tags_manager.py:231` 返回整棵标签树；`lora_networks.py:128` 每次全量遍历 `folder_paths` 扫盘。
16. **重复代码**（前端）：`lora_card.vue:967-990` 与 `lora_detail.vue:1162-1246` 几乎相同的图片缩放预览逻辑 → 抽 `useImagePreviewZoom`；浮动球设置的 localStorage 读写在三处重复（`setting_dialog.vue:456-485`、`FloatingBall.vue:43-78`、`App.vue:127-173`）→ 抽 `useFloatingBallSettings()`。
17. **v-html 注入面**：`prompt_index.vue:439` `v-html="t('promptBox.tagTips')"`，改用文本插值或做 sanitize。
18. **setInterval 未在卸载时清理**：`lora_index.vue:459-461` 仅在加载完成分支 clear，组件加载中卸载即泄漏。

---

## 四、低优先级（清理类）

19. `prompt_index.vue` 45 处 `console.*`；`:1801-1936`、`:3319-3402` 大段注释掉的死代码。
20. 模板内硬编码中文未走 i18n：`prompt_index.vue:589` `选中 {{n}} 个标签`。
21. 魔法数字：`FloatingBall.vue:52` 球径 `66`、`lora_card.vue:981` 缩放 `0.15~6`、`prompt_index.vue:1858` 超时 `30000`。
22. lora 卡片图片未懒加载（`lora_card.vue:253` 加 `loading="lazy"`）。
23. `prompt_server.py:1288` 用 `exec(compile(...))` 热重载，建议 `importlib.reload`。
24. `dao.py:26` `locale.getdefaultlocale()[0]` 可能为 `None`，`.startswith` 会崩。
25. 前端产物 660KB 单包（`dist/javascript/main.entry.js`），可路由级分包 + lodash/图标按需引入。

---

## 五、建议的修复路线

- **第一批（安全止血，1 天内）**：#2 路径穿越、#3 删 run_sql 接口、#6 origin 校验、#10 补超时。
- **第二批（稳定性，1 周内）**：#1 监听器泄漏、#4 阻塞改异步 + 哈希缓存、#5 连接模型、#18 定时器清理。
- **第三批（性能/可维护性）**：#7/#8 虚拟滚动与分页、#12 统一错误处理层、#16 抽 composable、#19-21 清理。

> 说明：本项目为自用 fork，ComfyUI 通常仅本地监听，安全风险的实际暴露面有限；但一旦端口对外（如局域网/公网访问 `--listen`），#2 #3 即为可直接利用的高危漏洞，建议优先收敛。
